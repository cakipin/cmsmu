---
// Halaman ini khusus untuk editor Puck (hanya diakses admin atau user dengan token jika butuh keamanan)
import ThemeEditorApp from "../components/ThemeEditorApp";

// Kita tidak ingin halaman ini diserap oleh SSG, jadi matikan prerender jika diperlukan (tapi Astro default di sini mungkin SSR karena Hono)
export const prerender = false;

// Ambil slug dari query params
const slug = Astro.url.searchParams.get("slug") || "about";
---

<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Theme Visual Editor - {slug}</title>
</head>
<body>
  <!-- Render aplikasi React Puck Editor -->
  <ThemeEditorApp client:only="react" slug={slug} />
</body>
</html>
